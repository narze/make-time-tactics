{
  "name": "with-jest",
  "version": "0.1.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test": "jest --watch",
    "test:ci": "jest --ci",
    "e2e": "start-test 'next dev -p 8080' 8080 cy:open",
    "e2e:ci": "start-test 'next dev -p 8080' 8080 'cypress run'",
    "cy:open": "cypress open"
  },
  "dependencies": {
    "next": "latest",
    "react": "16.12.0",
    "react-dom": "16.12.0"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^5.1.0",
    "@testing-library/react": "^9.4.0",
    "babel-jest": "^25.1.0",
    "cypress": "^4.9.0",
    "identity-obj-proxy": "^3.0.0",
    "jest": "^25.1.0",
    "react-test-renderer": "^16.12.0",
    "start-server-and-test": "^1.11.0"
  }
}
